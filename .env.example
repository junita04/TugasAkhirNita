# ============================================================
# Postgres Serving (untuk FastAPI lokal + metadata Superset)
# ============================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=academic_serving
POSTGRES_USER=academic
POSTGRES_PASSWORD=change-me
POSTGRES_SCHEMA=public

# ============================================================
# Superset
# ============================================================
SUPERSET_DB=superset
SUPERSET_ADMIN_USERNAME=admin
SUPERSET_ADMIN_EMAIL=admin@example.com
SUPERSET_ADMIN_PASSWORD=change-me
SUPERSET_SECRET_KEY=change-this-secret-key

# ============================================================
# MinIO (Object Storage Data Lake)
# ============================================================
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin-password
MINIO_BUCKET_WAREHOUSE=warehouse
MINIO_BUCKET_RAW=raw
MINIO_BUCKET_MODELS=models
MINIO_BUCKET_LOGS=logs

# Kredensial yang dipakai Spark/Trino untuk akses S3 (MinIO)
# Spark DIJALANKAN SEUTUHNYA DI DALAM DOCKER -> pakai service name 'minio'
# (resolve di network 'lakehouse', seperti yang dikonfig di docker-compose.yml).
# Jangan pakai localhost kecuali Spark sungguh-sungguh dijalankan di host.
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin-password
S3_ENDPOINT=http://minio:9000
S3_PATH_STYLE_ACCESS=true

# ============================================================
# Hive Metastore (metadata tabel Iceberg)
# ============================================================
# Spark di dalam Docker mengakses HMS via service name 'hive-metastore'.
HIVE_METASTORE_URI=thrift://hive-metastore:9083
POSTGRES_HIVE_HOST=postgres-hive
POSTGRES_HIVE_PORT=5433
POSTGRES_HIVE_DB=metastore
POSTGRES_HIVE_USER=hive
POSTGRES_HIVE_PASSWORD=hive-password

# ============================================================
# Spark
# ============================================================
SPARK_MASTER_HOST=spark-master
SPARK_MASTER_PORT=7077
SPARK_MASTER_UI=http://localhost:8080
SPARK_HISTORY_UI=http://localhost:18080

# Arsitektur target: Spark DIJALANKAN SEUTUHNYA DI DALAM DOCKER.
# 'cluster' = gunakan Spark Master dari Docker (spark://spark-master:7077).
# Jangan pakai SPARK_MODE=local untuk kebutuhan riset.
SPARK_MODE=cluster
# Untuk cluster: spark://spark-master:7077 (service docker-compose).
SPARK_MASTER_URL=spark://spark-master:7077
SPARK_LOCAL_DIRS=spark-tmp

# Warehouse & catalog Iceberg:
# - ICEBERG_CATALOG=local   -> folder filesystem 'iceberg/' (tanpa MinIO),
#                              SEHARUSNYA untuk Spark murni lokal.
# - ICEBERG_CATALOG=iceberg -> Hive Metastore + MinIO (s3a://warehouse/iceberg),
#                              terintegrasi dengan Trino/Superset.
# Pilih 'local' bila ingin ringan tanpa Docker Spark/MinIO;
# pilih 'iceberg' bila ingin integrasi penuh data lakehouse.
ICEBERG_CATALOG=iceberg
ICEBERG_WAREHOUSE=s3a://warehouse/iceberg

# ============================================================
# Trino (SQL Query Engine -> Superset)
# ============================================================
TRINO_URI=http://trino:8082
TRINO_HOST=trino
TRINO_PORT=8082
TRINO_CATALOG=iceberg
TRINO_USER=trino

# ============================================================
# Airflow (Orkestrasi pipeline)
# ============================================================
AIRFLOW_WEBSERVER_PORT=8085
AIRFLOW_ADMIN_USERNAME=admin
AIRFLOW_ADMIN_PASSWORD=admin-local-password
POSTGRES_AIRFLOW_HOST=postgres-airflow
POSTGRES_AIRFLOW_PORT=5434
POSTGRES_AIRFLOW_DB=airflow
POSTGRES_AIRFLOW_USER=airflow
POSTGRES_AIRFLOW_PASSWORD=airflow-password
