# ============================================================
# Mode Spark & Iceberg
# ============================================================
# Mode 'local'  : Spark local[*] + catalog Iceberg filesystem (folder iceberg/).
#                 Cocok untuk menjalankan pipeline di host tanpa Docker.
# Mode 'cluster': Spark Master Docker (spark://spark-master:7077) + catalog
#                 Hive Metastore + warehouse MinIO (s3a://warehouse/iceberg).
SPARK_MODE=local
SPARK_MASTER_URL=local[*]
ICEBERG_CATALOG=local
ICEBERG_WAREHOUSE=file:///D:/TA/TugasAkhirNita/iceberg

# ============================================================
# MinIO (Object Storage Data Lake) - hanya untuk mode cluster
# ============================================================
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin-password
MINIO_BUCKET_WAREHOUSE=warehouse
MINIO_BUCKET_RAW=raw
MINIO_BUCKET_MODELS=models
MINIO_BUCKET_LOGS=logs

# Kredensial Spark/Trino untuk akses S3 (MinIO). Dipakai hanya pada
# mode cluster (Spark di dalam Docker -> pakai service name 'minio').
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin-password
S3_ENDPOINT=http://minio:9000
S3_PATH_STYLE_ACCESS=true

# ============================================================
# Hive Metastore (metadata tabel Iceberg) - mode cluster
# ============================================================
HIVE_METASTORE_URI=thrift://hive-metastore:9083
POSTGRES_HIVE_HOST=postgres-hive
POSTGRES_HIVE_PORT=5433
POSTGRES_HIVE_DB=metastore
POSTGRES_HIVE_USER=hive
POSTGRES_HIVE_PASSWORD=hive-password

# ============================================================
# PostgreSQL Serving (opsional)
# ============================================================
# POSTGRES_ENABLED=true hanya bila ingin menyalin tabel Gold ke PostgreSQL
# (mis. konsumsi DB langsung). Superset membaca Iceberg melalui Trino,
# sehingga mode lokal cukup dengan POSTGRES_ENABLED=false.
POSTGRES_ENABLED=false
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=academic_serving
POSTGRES_USER=academic
POSTGRES_PASSWORD=change-me
POSTGRES_SCHEMA=public

# ============================================================
# Spark (mode cluster)
# ============================================================
SPARK_MASTER_HOST=spark-master
SPARK_MASTER_PORT=7077
SPARK_MASTER_UI=http://localhost:8080
SPARK_HISTORY_UI=http://localhost:18080
SPARK_LOCAL_DIRS=spark-tmp

# ============================================================
# Trino (SQL Query Engine -> Superset)
# ============================================================
TRINO_URI=http://trino:8082
TRINO_HOST=trino
TRINO_PORT=8082
TRINO_CATALOG=iceberg
TRINO_USER=trino

# ============================================================
# Superset
# ============================================================
SUPERSET_DB=superset
SUPERSET_ADMIN_USERNAME=admin
SUPERSET_ADMIN_EMAIL=admin@example.com
SUPERSET_ADMIN_PASSWORD=change-me
SUPERSET_SECRET_KEY=change-this-secret-key

# ============================================================
# Airflow (opsional, profile airflow)
# ============================================================
AIRFLOW_WEBSERVER_PORT=8085
AIRFLOW_ADMIN_USERNAME=admin
AIRFLOW_ADMIN_PASSWORD=admin-local-password
POSTGRES_AIRFLOW_HOST=postgres-airflow
POSTGRES_AIRFLOW_PORT=5434
POSTGRES_AIRFLOW_DB=airflow
POSTGRES_AIRFLOW_USER=airflow
POSTGRES_AIRFLOW_PASSWORD=airflow-password
